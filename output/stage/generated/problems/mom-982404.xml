<?xml version="1.0" encoding="utf-8"?>
<myopenmath xmlns:pi="http://pretextbook.org/2020/pretext/internal" id="982404">
<statement>
<p>Consider the following function which takes an array as input and returns a single numeric value as output. You may assume that all array values are small enough that they can be stored in a typical integer data type.
  </p>
<p>
   def sum<underscore/>array<underscore/>values(myArray):</p>
<p>
        total = 0
    </p>
<p>
        for element in myArray: </p>
<p>
          total = total + element
    </p>
<p>
        return total
  
  </p>
<p>
  Ignoring the operations to initialize total, to set up the for loop, and to return the result, how many operations must be completed when the function is run with myArray = <lbracket/>27, 25, 61, 72, 58, 96, 68, 57, 74, 94, 41<rbracket/>?</p>
<p><fillin characters="20" />

</p>
</statement>
<solution>
<p><ul><li>11</li></ul></p>
</solution>
</myopenmath>

